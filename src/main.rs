// use settings::Settings;

// mod settings;
// mod cli; 
mod llm_providers;

#[tokio::main]
async fn main() {

    // let settings = Settings::new();
    // cli::repl().await.expect("repl failed");
}
